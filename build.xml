<?xml version="1.0" encoding="UTF-8"?>
<project name="Lib - Xml" default="build">

    <property name="sourcedir" value="${basedir}/src/"/>
    <property name="testdir" value="${basedir}/Test/"/>

    <target name="build" depends="prepare,phpunit,phpdoc"/>

    <target name="clean" description="Cleanup build artifacts">
        <delete dir="${basedir}/build/coverage"/>
        <delete dir="${basedir}/build/logs"/>
    </target>

    <target name="prepare" depends="clean" description="Prepare for build">
        <mkdir dir="${basedir}/build/coverage"/>
        <mkdir dir="${basedir}/build/logs"/>
        <mkdir dir="${basedir}/build/docs"/>
    </target>

    <target name="phpunit" description="Run unit tests with PHPUnit">
        <exec dir="${basedir}" executable="phpunit" failonerror="true">
            <arg line="phpunit --junit ${basedir}/build/logs/log.junit.xml"/>
        </exec>
    </target>

    <target name="phpdoc" description="Generating technical documentation">
        <exec dir="${basedir}" executable="phpdoc" failonerror="true">
            <arg line="--target=build/docs/ --directory=src/ --title='Lib - Xml technical docs' --parseprivate --sourcecode"/>
        </exec>
    </target>

</project>